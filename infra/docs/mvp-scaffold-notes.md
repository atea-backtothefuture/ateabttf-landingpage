# MVP Scaffold Notes

## Frontend scope

- Public landing page with platform positioning and featured opportunities
- Opportunity listing route with starter filter chips
- Coordinator dashboard route with placeholder task and KPI cards

## Monorepo notes

- `apps/web` is ready for dependency installation and local Vite development
- `apps/api` is intentionally minimal so API and Wrangler ownership stays isolated
- Shared workspace settings live at the repo root for future package expansion
